export const APP_NAME = 'AcePick';

export const APP_COMPANY_NAME = 'AcePick';

export const DEFAULT_PAGINATION_SIZE = 5;

export const TEST_MODE = +(import.meta.env.VITE_TEST_MODE || 0) === 1 || false;

export const USER_DATA_KEY = APP_NAME + '_access_token';